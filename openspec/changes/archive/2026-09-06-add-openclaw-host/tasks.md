## 1. Confirm the supported native execution contract

- [x] 1.1 Pin an installable OpenClaw revision/version and trace public plugin registration, manifest tool declarations, synchronous factories, embedded warmup ordering and cleanup against the design's source evidence; record release differences without editing upstream.
- [x] 1.2 Exercise public before-tool `requireApproval` and synchronous `onResolution`, including final argument mutation after approval, absent approval route and cancellation; confirm the exact adapter-owned grant record contract before implementing dispatch.
- [x] 1.3 Verify a public ownership/cancellation path for activation still in progress, choose the finite warmup deadline consistent with hook policy, and demonstrate that skipped/timed-out hooks cannot expose late sessions or tools.
- [x] 1.4 Verify direct-user turn correlation, transient instruction/data projection across retries/tool continuations and mid-turn compaction on the chosen embedded route; explicitly record unsupported external harness/background/group paths.
- [x] 1.5 Freeze the existing closed capability profile for that route: per-turn context only where proved, session-start tools, native required approval, cooperative cancellation and only evidenced standard lifecycle kinds, excluding turn-committed.

## 2. Add optional MCP initial readiness

- [x] 2.1 Extend authored/normalized MCP root configuration with startupMode background|await-ready, default background, validating the complete row before external acquisition; cover omitted, valid, invalid and disabled-only configurations in the owning tests.
- [x] 2.2 Add generation-stable internal terminal startup outcomes distinct from cleanup promises, preserving concurrent launches, stage diagnostics, atomic owner publication and valid zero-tool success.
- [x] 2.3 Implement the strict initial-generation barrier and current-active revalidation; fail on startup error, timeout, cancellation, disposal or replacement rather than interpreting fulfilled start() promises as success.
- [x] 2.4 Await that barrier only during await-ready plugin apply after registering disposal ownership; retain early service publication, normal Loader audit and exhaustive failed-activation teardown without adding Composition Runtime readiness semantics.
- [x] 2.5 Preserve accepted in-place update and list-change refresh as background operations, retain unchanged server generations, fence retired ones before withdrawal, and apply the selected mode on fresh row insertion/recreation rather than retroactively on mode-only updates.
- [x] 2.6 Cover all-ready activation, a slow second server, a zero-tool server, failed sibling cleanup, initialization/discovery timeout, cancellation and stale completion through extension-mcp and real Composition Runtime tests.
- [x] 2.7 Run existing background startup, unavailable executable, per-server failure containment, refresh, replacement and disposal tests unchanged, including Composition Runtime's default non-awaiting MCP cases.

## 3. Add native package and prepared deployment artifacts

- [x] 3.1 Add the host-openclaw workspace package, public native plugin/preparation entrypoints, TypeScript configuration and explicit dependency-boundary manifest entries, consuming only neutral Doppelganger contracts and public OpenClaw APIs.
- [x] 3.2 Implement validated native options and immutable generated artifact schema for selected preset compatibility, concrete tool declarations, native/canonical mapping and descriptor contracts; exclude secrets, actor identity and process-local revisions from durable authorization.
- [x] 3.3 Implement one deterministic native-name/schema projection shared by preparation and runtime, rejecting collisions, unsupported schema semantics and host name limits before publishing tools.
- [x] 3.4 Implement preparation using the roster, ordered patches, audited disposable activation and ordinary catalog snapshot; do not inspect MCP services or silently change startupMode.
- [x] 3.5 Stage and atomically publish an installable generated deployment artifact only to the requested output, preserving prior output on failure and disposing temporary sessions before removing temporary roots.
- [x] 3.6 Cover valid preparation, awaited MCP discovery, empty compositions, incompatible names/schemas, unchanged authored files and cleanup on failure; verify package entrypoints from a clean installation closure with one Cordis root.

## 4. Implement native session ownership and activation

- [x] 4.1 Register hooks, finite tool factories and native cleanup synchronously; use no asynchronous registerTool calls, Promise factories, wildcard contracts or dispatcher fallback.
- [x] 4.2 Resolve selected Runtime Presets through the existing roster with normal precedence; distinguish defaultless inactivity, invalid selection and prepared-deployment mismatch without silently overriding operator selection.
- [x] 4.3 Implement the binding manager with immutable native agent/session generation, workspace and trusted principal custody; share one activation promise per binding and isolate concurrent sessions.
- [x] 4.4 Mount separate bound/unbound Actor Identity providers, reject guessed or mixed-sender bindings, and retire old epochs on reset, principal/workspace changes and session replacement.
- [x] 4.5 Implement bounded before-model warmup and independent factory/execute readiness guards; fence and dispose timed-out or late activations without claiming to block native host admission.
- [x] 4.6 Cover two-session isolation, concurrent warmup, defaultless activation, prepared mismatch, locked-model/skipped hook, timeout, identity rotation and late completion through native adapter entrypoints.

## 5. Project context and fixed-boundary tools

- [x] 5.1 Resolve budgeted portable context once per supported direct-user turn/run and preserve request/turn identity, accepted/omitted provenance and retry-safe cache ownership.
- [x] 5.2 Project instruction additions and transient user/data additions through ordinary before-prompt hooks; verify canonical transcript nonretention, tool continuation, retry reuse, failure omission and session separation.
- [x] 5.3 Bind ready immutable descriptors at synchronous native construction boundaries, preserving exact schema, canonical mapping, opaque revision and binding epoch in each closure.
- [x] 5.4 Handle toolCatalogChanged with monotonic pending snapshots and diagnostics only; reject removed/stale descriptors immediately and allow a compatible current revision only at a later native construction boundary.
- [x] 5.5 Diagnose undeclared names and incompatible prepared schema drift with explicit regeneration/restart instructions, preserving the active native catalog without private registry mutation or false mid-loop refresh claims.
- [x] 5.6 Cover prepared MCP and ordinary optional-feature tools, generated undeclared names, same-name replacement, removal, schema drift, delayed callbacks and reconstruction boundaries; retain full portable registry semantics independently of native limitations.

## 6. Enforce exact approval and cancellation

- [x] 6.1 Implement public before-tool requireApproval requests restricted to allow-once/deny and synchronous call-owned resolution records containing binding, run/call identity, name, revision and cloned input digest.
- [x] 6.2 Revalidate final native execute arguments, current descriptor, actor/workspace binding, epoch and cancellation before atomically consuming a resolution and minting the protected one-shot bridge grant.
- [x] 6.3 Map structured portable successes/domain failures separately from native adapter failures, preserving bounded result data and exact call correlation.
- [x] 6.4 Wire native AbortSignal to correlated bridge cancellation, remove listeners on settlement, and fence late approval/call completions after session disposal or replacement.
- [x] 6.5 Cover allow-once, denial, timeout, absent/headless route, input/revision mutation after approval, replay across calls/bindings, cancellation during approval and completion/cancellation races through the actual adapter dispatch path.

## 7. Preserve lifecycle, reload and exhaustive teardown

- [x] 7.1 Publish only evidenced normalized lifecycle kinds from adapter-owned transitions with bounded payloads and stable identities; never synthesize turn-committed from attempt completion, transcript writes or disposal.
- [x] 7.2 Cover capability absence for automatic capture and false-commit counterexamples while preserving explicit memory/recall tools and the existing selected OpenClaw context engine.
- [x] 7.3 Use Composition Runtime's existing serialized reload and audited rollback path; cover valid reload, invalid candidate restoration, restoration failure and native stale closures without adding a watcher or rewriting authored patches.
- [x] 7.4 Implement idempotent native reset/end/disable/shutdown cleanup: fence first, cancel calls and waits, unsubscribe, detach the bridge, dispose sessions/roots and aggregate failures after all reachable cleanup stages.
- [x] 7.5 Exercise disposal while activation or tool calls are held, failing disposers, repeated disposal and late callbacks, verifying sibling session isolation and no retained owned resources.

## 8. Verify adapter semantics and real native operation

- [x] 8.1 Adapt shared conformance test support for a fixed-capability native adapter with predeclared finite fixture names; preserve every existing OMP dynamic/transported case and actor-state coverage without a production protocol change.
- [x] 8.2 Run common isolation, empty protocols, immutable profile, revision/availability, approval replay, cancellation/completion, undeclared lifecycle and teardown cases through the real OpenClaw adapter; keep direct protocol-boundary actor absence separately labelled.
- [x] 8.3 Install a generated artifact in a temporary supported OpenClaw deployment, enable required conversation hooks, launch the actual embedded route and verify first-use awaited MCP tools plus instruction/data projection in real model requests.
- [x] 8.4 Exercise real native approval and denial, tool cancellation, two actor/workspace sessions, reset, reload and plugin/gateway shutdown; verify undeclared tools and false committed turns remain absent and record any unavailable external-service evidence explicitly.
- [x] 8.5 Run existing OMP activation, child/RPC, dynamic catalog, approval, context, lifecycle and shared conformance tests unchanged, and exercise the project-local OMP entrypoint with a temporary home and unchanged background MCP defaults.

## 9. Reconcile documentation and acceptance evidence

- [x] 9.1 Add the authoritative docs/hosts/openclaw.md with installation/preparation, trusted identity, hook permissions, capability matrix, regeneration boundary and limitations; update docs/README.md and setup usage links.
- [x] 9.2 Update MCP feature/configuration documentation for optional strict initial readiness, default background containment, in-place update semantics, timeout-versus-cleanup distinction and operator-owned acquisition.
- [x] 9.3 Update affected architecture, operations/verification, status/scope and source-pinned OpenClaw research owners to match implemented behavior; preserve DSH/memory change boundaries and do not label unproved routes supported.
- [x] 9.4 Replace every planned evidence row in this change with a direct unconditional executable test target proving its scenario; retain existing IDs and avoid duplicate live ownership when reconciling deltas.
- [x] 9.5 Run openspec validate add-openclaw-host --strict, npm run check:focused-specs:change -- add-openclaw-host, focused change evidence and npm run check; resolve failures without narrowing native or OMP acceptance.
- [x] 9.6 Run npm run check:security for introduced dependency changes, report unresolved reviewed advisories accurately, and attach native smoke/packaging evidence with the exact tested host revision before declaring support.
